dist: bionic
addons:
    ssh_known_hosts: 148.60.11.22
services:
  - docker    
env:
  - DOCKER_COMPOSE_VERSION=1.27.4
sudo: required

before_install:
  - sudo apt update
  - openssl aes-256-cbc -K $encrypted_<...>_key -iv $encrypted_<...>_iv -in deploy_rsa.enc -out /tmp/deploy_rsa -d
  - eval "$(ssh-agent -s)"
  - chmod 600 /tmp/deploy_rsa
  - ssh-add /tmp/deploy_rsa
  - sudo ufw allow ssh
  - sudo apt-get install -y ppp
  - sudo apt install sshpass
  - sudo apt install openfortivpn
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin  
script:
 #- cd api/      
 # - mvn package -DskipTests
 # - cd ../
  - docker pull frn18/tlc:quarkus-back-jvm
  - docker pull frn18/tlc:bunkerized-nginx-front
  - docker-compose up -d
deploy:
  provider: script
  skip_cleanup: true
  script: sudo openfortivpn istic-vpn.univ-rennes1.fr:443 -u fleroy -p "${VPN_PASS}" --trusted-cert bf01c9bcba70b3be1f8ae8f941afbc7dcf1d8c81afe7e3d10f48e460c44446c6 --no-routes --no-dns --pppd-no-peerdns &
&& rsync -r --delete-after --quiet / zprojet@148.60.11.22:/home/doodlestudent
  on:
    branch: master
#after_sucess:
  #- find / -type f -exec curl -u xxx:psw --ftp-create-dirs -T {} ftp://148.60.11.226/home/doodlestudent/{} \;
  #- sudo openfortivpn istic-vpn.univ-rennes1.fr:443 -u fleroy -p "${VPN_PASS}" --trusted-cert bf01c9bcba70b3be1f8ae8f941afbc7dcf1d8c81afe7e3d10f48e460c44446c6 --no-routes --no-dns --pppd-no-peerdns &
  #- sleep 15
  #- ssh zprojet@148.60.11.226
  #- sshpass -p ${SFTP_PASSWORD} ssh zprojet@148.60.11.226
  #- echo "${SFTP_KEY}" | base64 --decode >/tmp/sftp_rsa
  #- export SSHPASS=${SFTP_PASSWORD}       
  #- sshpass -e sftp -oBatchMode=no -b - zprojet@148.60.11.226
  #- cd ~/doodlestudents
  #- put -r / 
  #- sshpass -e ssh ${SFTP_USER}@148.60.11.226 /home/doodlestudent/buildAndRun.sh  
